{
	"name": "widehook-monorepo",
	"private": true,
	"workspaces": [
		"packages/*",
		"examples/*"
	],
	"scripts": {
		"dev:example": "yarn workspace example dev",
		"dev:example1": "yarn workspace example1 dev",
		"build": "yarn workspace widehook build",
		"build:example": "yarn workspace example build",
		"build:example1": "yarn workspace example1 build",
		"lint": "yarn workspaces foreach run lint",
		"type-check": "yarn workspaces foreach run type-check"
	},
	"devDependencies": {
		"@changesets/cli": "^2.27.9",
		"husky": "^9.1.6",
		"prettier": "^3.3.3",
		"pretty-quick": "^4.0.0"
	},
	"packageManager": "yarn@4.5.1"
}
